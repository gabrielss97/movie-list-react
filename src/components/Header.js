import { MainMenu } from "./MainMenu";

export const Header = () => {
  return (
    <section>
      <h1 className="main-header">Bem vindo ao Movie List</h1>
      <MainMenu />
    </section>
  );
};
